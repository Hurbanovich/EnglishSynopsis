<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.kreva.englishsynopsis">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.EnglishSynopsis">
        <activity android:name=".activities.SpellingOfWordsActivity">
        </activity>
        <activity android:name=".activities.AllWordActivity">
        </activity>
        <activity android:name=".activities.MemorizingRussianActivity">
        </activity>
        <activity android:name=".activities.MemorizingEnglishActivity">
        </activity>
        <activity android:name=".activities.NewWordActivity">
        </activity>
        <activity
                android:icon="@drawable/qwerty"
                android:name=".activities.MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>